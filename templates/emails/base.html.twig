<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}FormBuilder{% endblock %}</title>
    <style>
        /* Reset et styles de base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background-color: #1f1b1a;
            color: #ffffff;
            line-height: 1.6;
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
            margin: 0;
            padding: 20px;
        }
        
        /* Conteneur principal */
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #1f1b1a;
            border-radius: 8px;
            overflow: hidden;
        }
        
        /* Header */
        .email-header {
            background-color: #1f1b1a;
            padding: 24px 24px 16px 24px;
            text-align: left;
            border-bottom: 1px solid #404040;
        }
        
        .email-header h1 {
            color: #ffffff;
            font-size: 1.5rem;
            font-weight: 600;
            margin: 0;
            line-height: 1.2;
        }
        
        .email-header .subtitle {
            color: #a3a3a3;
            font-size: 0.875rem;
            font-weight: 400;
            margin-top: 4px;
        }
        
        /* Contenu principal */
        .email-content {
            padding: 24px;
            background-color: #1f1b1a;
        }
        
        .email-content h2 {
            color: #ffffff;
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 16px;
            line-height: 1.3;
        }
        
        .email-content h3 {
            color: #ffffff;
            font-size: 1.125rem;
            font-weight: 600;
            margin-bottom: 12px;
            line-height: 1.3;
        }
        
        .email-content p {
            color: #a3a3a3;
            font-size: 0.875rem;
            margin-bottom: 16px;
            line-height: 1.5;
        }
        
        /* Boutons */
        .button-primary {
            display: inline-block !important;
            background-color: #eab308 !important;
            color: #000000 !important;
            text-decoration: none !important;
            border-radius: 8px !important;
            padding: 10px 20px !important;
            font-weight: 500 !important;
            font-size: 0.875rem !important;
            transition: all 0.2s ease-in-out;
            border: none !important;
            margin: 16px 0 !important;
        }
        
        .button-primary:hover {
            color: #000000 !important;
            background-color: #facc15 !important;
        }
        
        .button-primary:visited {
            color: #000000 !important;
        }
        
        .button-primary:active {
            color: #000000 !important;
        }
        
        /* Listes */
        .email-content ul {
            margin: 16px 0;
            padding-left: 16px;
        }
        
        .email-content li {
            color: #a3a3a3;
            margin-bottom: 6px;
            font-size: 0.875rem;
        }
        
        /* Footer */
        .email-footer {
            background-color: #1f1b1a;
            padding: 20px 24px;
            text-align: center;
            border-top: 1px solid #404040;
        }
        
        .email-footer p {
            color: #737373;
            font-size: 0.75rem;
            margin-bottom: 6px;
        }
        
        .email-footer a {
            color: #eab308;
            text-decoration: none;
        }
        
        /* Responsive */
        @media only screen and (max-width: 600px) {
            body {
                padding: 10px;
            }
            
            .email-container {
                margin: 0;
                border-radius: 8px;
            }
            
            .email-header,
            .email-content,
            .email-footer {
                padding: 20px 16px;
            }
            
            .email-header h1 {
                font-size: 1.25rem;
            }
            
            .email-content h2 {
                font-size: 1.125rem;
            }
            
            .button-primary {
                display: block;
                text-align: center;
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="email-header">
            <h1>{% block header_title %}FormBuilder{% endblock %}</h1>
            {% if block('header_subtitle') is defined %}
                <div class="subtitle">{% block header_subtitle %}{% endblock %}</div>
            {% endif %}
        </div>
        
        <!-- Contenu principal -->
        <div class="email-content">
            {% block content %}{% endblock %}
        </div>
        
        <!-- Footer -->
        <div class="email-footer">
            <p>© 2025 <strong style="color: #eab308;">FormBuilder</strong>. Tous droits réservés.</p>
            <p>Si vous n'avez pas demandé cet email, vous pouvez l'ignorer en toute sécurité.</p>
            {% if block('footer_links') is defined %}
                <div style="margin-top: 12px;">
                    {% block footer_links %}{% endblock %}
                </div>
            {% endif %}
        </div>
    </div>
</body>
</html>
